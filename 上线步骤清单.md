# 上线步骤清单

## 项目概述
求职助手应用前后端功能同步修复上线指南

## 上线时间
2024年1月1日

## 上线前准备

### 1. 环境检查
- [ ] 确认生产环境数据库连接正常
- [ ] 确认后端服务运行正常
- [ ] 确认前端服务运行正常
- [ ] 确认缓存服务运行正常
- [ ] 确认文件存储服务运行正常

### 2. 数据备份
- [ ] 备份数据库
- [ ] 备份配置文件
- [ ] 备份静态资源
- [ ] 备份用户上传文件

### 3. 代码检查
- [ ] 确认所有修复代码已提交
- [ ] 确认代码已通过CI/CD检查
- [ ] 确认测试用例已通过
- [ ] 确认代码已review

## 上线步骤

### 阶段一：后端部署

#### 1. 停止服务
```bash
# 停止后端服务
pm2 stop app
```

#### 2. 备份当前版本
```bash
# 备份当前版本
cp -r /path/to/app /backup/app-$(date +%Y%m%d-%H%M%S)
```

#### 3. 更新代码
```bash
# 切换到生产环境
cd /path/to/app

# 拉取最新代码
git pull origin main

# 安装依赖
pip install -r requirements.txt

# 运行数据库迁移（如果有）
python -m alembic upgrade head
```

#### 4. 启动服务
```bash
# 启动后端服务
pm2 start app --name "app-prod"

# 检查服务状态
pm2 status
```

#### 5. 验证后端服务
```bash
# 检查API健康状态
curl -X GET http://localhost:8000/health

# 测试关键接口
curl -X GET http://localhost:8000/api/v1/auth/me
curl -X GET http://localhost:8000/api/v1/jobs/list
curl -X GET http://localhost:8000/api/v1/questions/list
```

### 阶段二：前端部署

#### 1. 停止前端服务
```bash
# 停止前端服务
pm2 stop frontend
```

#### 2. 备份当前版本
```bash
# 备份当前版本
cp -r /path/to/frontend /backup/frontend-$(date +%Y%m%d-%H%M%S)
```

#### 3. 更新代码
```bash
# 切换到生产环境
cd /path/to/frontend

# 拉取最新代码
git pull origin main

# 安装依赖
npm install

# 构建生产版本
npm run build
```

#### 4. 启动服务
```bash
# 启动前端服务
pm2 start npm --name "frontend-prod" -- start

# 检查服务状态
pm2 status
```

#### 5. 验证前端服务
```bash
# 检查前端服务状态
curl -I http://localhost:3000

# 检查关键页面
curl -s http://localhost:3000 | grep -q "求职助手"
```

### 阶段三：验证测试

#### 1. 功能测试
- [ ] 用户注册功能
- [ ] 用户登录功能
- [ ] 岗位搜索功能
- [ ] 岗位详情功能
- [ ] 收藏功能
- [ ] 题目功能
- [ ] 工作台功能
- [ ] 仪表板功能

#### 2. 数据验证
- [ ] 用户数据正确显示
- [ ] 岗位数据正确显示
- [ ] 题目数据正确显示
- [ ] 投递记录正确显示
- [ ] 统计数据正确显示

#### 3. 性能测试
- [ ] 页面加载时间 ≤ 3秒
- [ ] API响应时间 ≤ 1.5秒
- [ ] 数据库查询时间 ≤ 1秒

#### 4. 兼容性测试
- [ ] Chrome浏览器
- [ ] Firefox浏览器
- [ ] Safari浏览器
- [ ] 移动端浏览器

### 阶段四：监控设置

#### 1. 应用监控
- [ ] 设置应用性能监控
- [ ] 设置错误监控
- [ ] 设置用户行为监控

#### 2. 系统监控
- [ ] 设置CPU监控
- [ ] 设置内存监控
- [ ] 设置磁盘监控
- [ ] 设置网络监控

#### 3. 业务监控
- [ ] 设置用户活跃度监控
- [ ] 设置API调用监控
- [ ] 设置错误率监控

## 上线后检查

### 1. 实时监控
- [ ] 检查服务状态
- [ ] 检查错误日志
- [ ] 检查性能指标
- [ ] 检查用户反馈

### 2. 数据检查
- [ ] 检查数据一致性
- [ ] 检查数据完整性
- [ ] 检查数据准确性

### 3. 用户反馈
- [ ] 收集用户反馈
- [ ] 处理用户问题
- [ ] 回应用户疑问

## 回滚方案

### 1. 快速回滚
```bash
# 回滚后端
pm2 stop app-prod
cp -r /backup/app-$(date +%Y%m%d-%H%M%S) /path/to/app
pm2 start app --name "app-prod"

# 回滚前端
pm2 stop frontend-prod
cp -r /backup/frontend-$(date +%Y%m%d-%H%M%S) /path/to/frontend
pm2 start npm --name "frontend-prod" -- start
```

### 2. 数据回滚
```bash
# 恢复数据库
pg_restore -h localhost -U postgres -d app_db /backup/app_db-$(date +%Y%m%d-%H%M%S).dump
```

### 3. 紧急联系
- [ ] 技术负责人：138-0000-0000
- [ ] 运维负责人：138-0000-0001
- [ ] 产品负责人：138-0000-0002

## 上线后维护

### 1. 日常维护
- [ ] 检查服务状态
- [ ] 检查日志文件
- [ ] 检查性能指标
- [ ] 备份数据

### 2. 定期维护
- [ ] 每周：清理日志文件
- [ ] 每月：数据库优化
- [ ] 每季度：系统升级
- [ ] 每年：架构评估

### 3. 应急处理
- [ ] 制定应急预案
- [ ] 准备应急工具
- [ ] 定期应急演练
- [ ] 更新应急文档

## 风险评估

### 1. 高风险
- [ ] 数据库连接失败
- [ ] 服务启动失败
- [ ] 数据损坏

### 2. 中风险
- [ ] 性能下降
- [ ] 功能异常
- [ ] 用户体验下降

### 3. 低风险
- [ ] 界面样式问题
- [ ] 小功能异常
- [ ] 文档更新

## 成功标准

### 1. 技术指标
- [ ] 服务可用性 ≥ 99.9%
- [ ] 响应时间 ≤ 1.5秒
- [ ] 错误率 ≤ 0.1%
- [ ] 数据一致性 100%

### 2. 业务指标
- [ ] 用户活跃度 ≥ 80%
- [ ] 功能使用率 ≥ 90%
- [ ] 用户满意度 ≥ 90%
- [ ] 业务目标达成率 ≥ 95%

### 3. 运营指标
- [ ] 系统稳定性 ≥ 99.9%
- [ ] 故障恢复时间 ≤ 30分钟
- [ ] 数据备份成功率 100%
- [ ] 监控覆盖率 100%

## 总结

本次上线涉及的主要变更：
1. 后端数据结构修复
2. 前端数据访问路径修正
3. 接口格式统一
4. 测试体系完善

所有变更已通过测试验证，具备上线条件。请按照上述步骤执行上线操作，确保系统稳定运行。