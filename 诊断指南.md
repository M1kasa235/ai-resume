# 前后端联通性诊断与修复指南

## 🔍 问题诊断步骤

### 步骤 1: 检查后端服务是否运行

#### 方法 1: 浏览器访问
打开浏览器访问以下地址:
- **健康检查**: http://localhost:8002/health
- **API 文档**: http://localhost:8002/docs

如果能看到响应,说明后端正在运行。

#### 方法 2: 命令行测试
```bash
# Windows PowerShell
Invoke-WebRequest -Uri http://localhost:8002/health

# 或使用 curl (如果已安装)
curl http://localhost:8002/health
```

---

### 步骤 2: 启动后端服务

如果后端未运行,按以下步骤启动:

```bash
# 1. 进入项目根目录
cd "C:\Users\Administrator\Desktop\ai assume"

# 2. 确保依赖已安装
pip install -r requirements.txt

# 3. 检查 .env 文件是否存在
# 如果没有,复制示例文件
copy .env.example .env

# 4. 启动后端服务
python run.py
```

**预期输出:**
```
INFO:     Uvicorn running on http://127.0.0.1:8002
INFO:     Application startup complete.
```

---

### 步骤 3: 检查数据库连接

后端需要 MySQL 数据库,确认:

1. **MySQL 服务是否运行**
   ```bash
   # Windows: 检查 MySQL 服务
   services.msc
   # 找到 MySQL80 或类似服务,确保状态为"正在运行"
   ```

2. **数据库配置是否正确** (在 `.env` 文件中)
   ```env
   MYSQL_HOST=localhost
   MYSQL_PORT=3306
   MYSQL_USER=root
   MYSQL_PASSWORD=mysql80
   MYSQL_DB=ai_job
   ```

3. **数据库是否存在**
   ```sql
   -- 连接到 MySQL
   mysql -u root -p
   
   -- 创建数据库(如果不存在)
   CREATE DATABASE IF NOT EXISTS ai_job CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

---

### 步骤 4: 检查前端配置

#### 1. API 地址配置
文件: `frontend/.env.development`
```env
VITE_API_BASE_URL=http://localhost:8002
VITE_ENABLE_MOCK=false
```

#### 2. 重启前端服务
修改配置后必须重启:
```bash
cd "C:\Users\Administrator\Desktop\ai assume\frontend"
npm run dev
```

---

### 步骤 5: 测试注册功能

#### 方法 1: 使用 Swagger UI
1. 访问: http://localhost:8002/docs
2. 找到 `/api/v1/auth/register` 接口
3. 点击 "Try it out"
4. 填写测试数据:
   ```json
   {
     "username": "testuser",
     "email": "test@example.com",
     "password": "123456"
   }
   ```
5. 点击 "Execute"
6. 查看响应状态码是否为 201

#### 方法 2: 使用 Postman/Apifox
**请求:**
- 方法: POST
- URL: http://localhost:8002/api/v1/auth/register
- Headers: Content-Type: application/json
- Body (raw JSON):
  ```json
  {
    "username": "testuser",
    "email": "test@example.com",
    "password": "123456"
  }
  ```

**预期响应:**
```json
{
  "id": 1,
  "username": "testuser",
  "email": "test@example.com",
  "created_at": "2024-01-01T00:00:00"
}
```

---

### 步骤 6: 测试登录功能

#### 方法 1: Swagger UI
1. 访问: http://localhost:8002/docs
2. 找到 `/api/v1/auth/login` 接口
3. 填写测试数据:
   ```json
   {
     "username": "testuser",
     "password": "123456"
   }
   ```
4. 执行并查看响应

**预期响应:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "refresh_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "bearer",
  "expires_in": 1800
}
```

---

## 🛠️ 常见问题与解决方案

### 问题 1: Connection Refused (连接被拒绝)

**症状:** 
- 浏览器显示 "ERR_CONNECTION_REFUSED"
- Network 面板显示请求失败

**原因:** 后端服务未启动

**解决方案:**
```bash
cd "C:\Users\Administrator\Desktop\ai assume"
python run.py
```

---

### 问题 2: CORS 跨域错误

**症状:**
Console 显示:
```
Access to fetch at 'http://localhost:8002' from origin 'http://localhost:5173' 
has been blocked by CORS policy
```

**原因:** CORS 配置不正确

**解决方案:**
检查 `app/core/config.py` 中的 `CORS_ORIGINS`:
```python
CORS_ORIGINS: List[str] = ["http://localhost:5173", "http://localhost:3000"]
```

如果前端端口不是 5173,需要添加对应端口。

---

### 问题 3: 数据库连接失败

**症状:**
后端启动时报错:
```
Can't connect to MySQL server
```

**解决方案:**
1. 启动 MySQL 服务
2. 检查 `.env` 中的数据库配置
3. 确认数据库 `ai_job` 已创建

---

### 问题 4: 404 Not Found

**症状:**
API 返回 404

**原因:** API 路径错误

**正确路径:**
- 注册: `POST http://localhost:8002/api/v1/auth/register`
- 登录: `POST http://localhost:8002/api/v1/auth/login`
- 获取用户信息: `GET http://localhost:8002/api/v1/auth/me`

注意: 所有 API 都有 `/api/v1` 前缀!

---

### 问题 5: Mock 数据干扰

**症状:**
前端显示的数据不是来自后端

**解决方案:**
修改 `frontend/.env.development`:
```env
VITE_ENABLE_MOCK=false
```
然后重启前端服务。

---

### 问题 6: 端口冲突

**症状:**
```
ERROR: [Errno 10048] error while attempting to bind on address
```

**解决方案:**
1. 查找占用端口的进程:
   ```bash
   # Windows
   netstat -ano | findstr :8002
   ```

2. 终止进程或更换端口:
   ```bash
   # 终止进程 (PID 替换为实际值)
   taskkill /F /PID 12345
   
   # 或修改 run.py 中的端口
   port=8003
   ```

---

## ✅ 验证清单

完成以下步骤确认联通性正常:

- [ ] 后端服务正在运行 (访问 http://localhost:8002/health 返回 ok)
- [ ] 前端服务正在运行 (访问 http://localhost:5173 看到登录页)
- [ ] Swagger 文档可访问 (http://localhost:8002/docs)
- [ ] 能够成功注册用户 (通过 Swagger 或前端页面)
- [ ] 能够成功登录 (获得 access_token 和 refresh_token)
- [ ] 浏览器 Console 没有 CORS 错误
- [ ] Network 面板中请求状态码为 2xx

---

## 🚀 快速启动脚本

### Windows PowerShell (start-backend.ps1)
```powershell
# 启动后端服务
Set-Location "C:\Users\Administrator\Desktop\ai assume"
Write-Host "正在启动后端服务..." -ForegroundColor Green
python run.py
```

### Windows PowerShell (start-frontend.ps1)
```powershell
# 启动前端服务
Set-Location "C:\Users\Administrator\Desktop\ai assume\frontend"
Write-Host "正在启动前端服务..." -ForegroundColor Green
npm run dev
```

---

## 📞 仍然无法解决?

请提供以下信息:

1. **后端日志**: 启动 `run.py` 后的完整输出
2. **前端控制台错误**: 浏览器 F12 -> Console 标签的错误信息
3. **Network 请求详情**: 
   - 请求 URL
   - 请求方法
   - 状态码
   - 响应内容
4. **测试结果**: 
   - http://localhost:8002/health 是否能访问
   - http://localhost:8002/docs 是否能访问
