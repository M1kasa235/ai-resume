# 前后端接口对照表

## 概述
本文档详细记录了前端与后端API接口的对应关系，确保前后端功能完全对齐。

## 接口规范说明

### 通用规范
- **基础URL**: `/api/v1`
- **认证方式**: Bearer Token (JWT)
- **请求头**: `Content-Type: application/json`
- **响应格式**: 统一JSON格式
- **分页格式**: `{ total, items, page, page_size }`

### 错误码对照
| HTTP状态码 | 错误类型 | 说明 |
|------------|----------|------|
| 200 | SUCCESS | 请求成功 |
| 201 | CREATED | 资源创建成功 |
| 400 | BAD_REQUEST | 请求参数错误 |
| 401 | UNAUTHORIZED | 未授权或Token过期 |
| 403 | FORBIDDEN | 权限不足 |
| 404 | NOT_FOUND | 资源不存在 |
| 422 | UNPROCESSABLE_ENTITY | 数据验证失败 |
| 500 | INTERNAL_ERROR | 服务器内部错误 |

## 详细接口对照

### 1. 认证相关接口

#### 1.1 用户注册
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `authApi.register()` | `POST /auth/register` | ✅ 已对齐 |
| **请求方法** | POST | POST | ✅ |
| **请求参数** | `{ username, email, phone?, password }` | `{ username, email, phone?, password }` | ✅ |
| **响应格式** | `ApiResponse<UserResponse>` | `UserResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 1.2 用户登录
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `authApi.login()` | `POST /auth/login` | ✅ 已对齐 |
| **请求方法** | POST | POST | ✅ |
| **请求参数** | `{ username, password }` | `{ username, password }` | ✅ |
| **响应格式** | `ApiResponse<Token>` | `Token` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 1.3 刷新Token
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `authApi.refreshToken()` | `POST /auth/refresh` | ✅ 已对齐 |
| **请求方法** | POST | POST | ✅ |
| **请求参数** | `{ refresh_token }` | `{ refresh_token }` | ✅ |
| **响应格式** | `ApiResponse<Token>` | `Token` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 1.4 获取当前用户信息
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `authApi.getCurrentUser()` | `GET /auth/me` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse<UserResponse>` | `UserResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 1.5 用户登出
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `authApi.logout()` | `POST /auth/logout` | ✅ 已对齐 |
| **请求方法** | POST | POST | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse` | `{ message }` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

### 2. 用户管理接口

#### 2.1 获取个人资料
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `userApi.getProfile()` | `GET /users/profile` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse<UserResponse>` | `UserResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 2.2 更新个人资料
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `userApi.updateProfile()` | `PUT /users/profile` | ✅ 已对齐 |
| **请求方法** | PUT | PUT | ✅ |
| **请求参数** | `{ real_name?, gender?, current_city?, target_city?, work_years?, education?, avatar_url? }` | `{ real_name?, gender?, current_city?, target_city?, work_years?, education?, avatar_url? }` | ✅ |
| **响应格式** | `ApiResponse<UserResponse>` | `UserResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 2.3 修改密码
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `userApi.changePassword()` | `PUT /users/password` | ✅ 已对齐 |
| **请求方法** | PUT | PUT | ✅ |
| **请求参数** | `{ old_password, new_password }` | `{ old_password, new_password }` | ✅ |
| **响应格式** | `ApiResponse` | `{ message }` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

### 3. 岗位管理接口

#### 3.1 获取岗位列表
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `jobApi.getJobs()` | `GET /jobs/list` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | `{ keyword?, city?, category_id?, salary_min?, salary_max?, experience_min?, experience_max?, education?, company_stage?, skills?, sort_by?, sort_order?, page?, page_size?, only_urgent? }` | `{ keyword?, city?, category_id?, salary_min?, salary_max?, experience_min?, experience_max?, education?, company_stage?, skills?, sort_by?, sort_order?, page?, page_size?, only_urgent? }` | ✅ |
| **响应格式** | `PaginatedResponse<Job>` | `PageResponse[JobResponse]` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 3.2 获取热门岗位
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `jobApi.getHotJobs()` | `GET /jobs/hot` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | `{ limit? }` | `{ limit? }` | ✅ |
| **响应格式** | `ApiResponse<Job[]>` | `Job[]` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 3.3 获取最新岗位
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `jobApi.getLatestJobs()` | `GET /jobs/latest` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | `{ limit? }` | `{ limit? }` | ✅ |
| **响应格式** | `ApiResponse<Job[]>` | `Job[]` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 3.4 获取岗位统计数据
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `jobApi.getJobStatistics()` | `GET /jobs/statistics` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse` | 统计数据对象 | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 3.5 获取岗位详情
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `jobApi.getJobDetail(id)` | `GET /jobs/{id}` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse<Job>` | `JobResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 3.6 创建岗位
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `jobApi.createJob()` | `POST /jobs` | ✅ 已对齐 |
| **请求方法** | POST | POST | ✅ |
| **请求参数** | `{ title, company_name, category_id?, company_logo?, company_stage?, company_size?, description?, requirements?, salary_min?, salary_max?, salary_months?, city?, district?, address?, experience_min?, experience_max?, education_requirement?, skills_required?, tags?, is_urgent?, expired_at? }` | `{ title, company_name, category_id?, company_logo?, company_stage?, company_size?, description?, requirements?, salary_min?, salary_max?, salary_months?, city?, district?, address?, experience_min?, experience_max?, education_requirement?, skills_required?, tags?, is_urgent?, expired_at? }` | ✅ |
| **响应格式** | `ApiResponse<Job>` | `JobResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 3.7 更新岗位
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `jobApi.updateJob(id, data)` | `PUT /jobs/{id}` | ✅ 已对齐 |
| **请求方法** | PUT | PUT | ✅ |
| **请求参数** | `Partial<JobCreate>` | `Partial<JobUpdate>` | ✅ |
| **响应格式** | `ApiResponse<Job>` | `JobResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 3.8 删除岗位
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `jobApi.deleteJob(id)` | `DELETE /jobs/{id}` | ✅ 已对齐 |
| **请求方法** | DELETE | DELETE | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse` | `{ message }` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 3.9 岗位分类相关
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **获取分类树** | `jobApi.getCategoryTree()` | `GET /jobs/categories/tree` | ✅ 已对齐 |
| **获取分类列表** | `jobApi.getCategoryList()` | `GET /jobs/categories/list` | ✅ 已对齐 |
| **创建分类** | `jobApi.createCategory()` | `POST /jobs/categories` | ✅ 已对齐 |
| **更新分类** | `jobApi.updateCategory(id, data)` | `PUT /jobs/categories/{id}` | ✅ 已对齐 |
| **删除分类** | `jobApi.deleteCategory(id)` | `DELETE /jobs/categories/{id}` | ✅ 已对齐 |

#### 3.10 收藏相关
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **添加收藏** | `jobApi.addFavorite(jobId)` | `POST /jobs/{jobId}/favorite` | ✅ 已对齐 |
| **取消收藏** | `jobApi.removeFavorite(jobId)` | `DELETE /jobs/{jobId}/favorite` | ✅ 已对齐 |
| **获取收藏列表** | `jobApi.getMyFavorites()` | `GET /jobs/user/favorites` | ✅ 已对齐 |
| **检查收藏状态** | `jobApi.checkFavoriteStatus(jobId)` | `GET /jobs/{jobId}/favorite/status` | ✅ 已对齐 |

### 4. 题库接口

#### 4.1 获取题目列表
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `questionApi.getQuestions()` | `GET /questions` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | `{ category_id?, difficulty?, question_type?, keyword?, only_hot?, page?, page_size? }` | `{ category_id?, difficulty?, question_type?, keyword?, only_hot?, page?, page_size? }` | ✅ |
| **响应格式** | `PaginatedResponse<Question>` | `QuestionListResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 4.2 获取题目详情
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `questionApi.getQuestionDetail(id)` | `GET /questions/{id}` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse<QuestionDetail>` | `QuestionDetail` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 4.3 提交答案
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `questionApi.submitAnswer(questionId, data)` | `POST /questions/{questionId}/answer` | ✅ 已对齐 |
| **请求方法** | POST | POST | ✅ |
| **请求参数** | `{ answer, time_spent? }` | `{ answer, time_spent? }` | ✅ |
| **响应格式** | `ApiResponse<AnswerSubmitResponse>` | `AnswerSubmitResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 4.4 获取刷题统计
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `questionApi.getPracticeStats()` | `GET /questions/my/stats` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse<PracticeStats>` | `PracticeStats` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 4.5 获取错题列表
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `questionApi.getWrongQuestions()` | `GET /questions/my/wrong` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | `{ page?, page_size? }` | `{ page?, page_size? }` | ✅ |
| **响应格式** | `PaginatedResponse<WrongQuestionItem>` | `WrongQuestionListResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 4.6 标记错题为已掌握
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `questionApi.markWrongAsMastered(wrongId)` | `PUT /questions/my/wrong/{wrongId}/mastered` | ✅ 已对齐 |
| **请求方法** | PUT | PUT | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse` | 成功响应 | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 4.7 收藏相关
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **获取收藏列表** | `questionApi.getFavorites()` | `GET /questions/my/favorites` | ✅ 已对齐 |
| **收藏/取消收藏** | `questionApi.toggleFavorite(questionId)` | `POST /questions/{questionId}/favorite` | ✅ 已对齐 |

### 5. 工作台接口

#### 5.1 上传简历
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `workbenchApi.uploadResume(file)` | `POST /workbench/resume/upload` | ✅ 已对齐 |
| **请求方法** | POST | POST | ✅ |
| **请求参数** | `FormData` | `FormData` | ✅ |
| **响应格式** | `ApiResponse<UploadResumeResponse>` | `UploadResumeResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 5.2 获取简历信息
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `workbenchApi.getResumeInfo()` | `GET /workbench/resume` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse<ResumeInfo>` | `ResumeInfo` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 5.3 获取投递记录
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `workbenchApi.getApplications()` | `GET /workbench/applications` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | `{ page?, size? }` | `{ page?, size? }` | ✅ |
| **响应格式** | `PaginatedResponse<ApplicationItem>` | `ApplicationListResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

### 6. 首页接口

#### 6.1 获取首页概览
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `dashboardApi.getOverview()` | `GET /dashboard/overview` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | 无 | 无 | ✅ |
| **响应格式** | `ApiResponse<DashboardOverviewResponse>` | `DashboardOverviewResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 6.2 获取成长曲线
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `dashboardApi.getGrowthCurve(days)` | `GET /dashboard/growth-curve` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | `{ days? }` | `{ days? }` | ✅ |
| **响应格式** | `ApiResponse<GrowthCurveResponse>` | `GrowthCurveResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

#### 6.3 获取最新动态
| 项目 | 前端调用 | 后端接口 | 状态 |
|------|----------|----------|------|
| **路径** | `dashboardApi.getActivities(limit)` | `GET /dashboard/activities` | ✅ 已对齐 |
| **请求方法** | GET | GET | ✅ |
| **请求参数** | `{ limit? }` | `{ limit? }` | ✅ |
| **响应格式** | `ApiResponse<ActivitiesResponse>` | `ActivitiesResponse` | ✅ |
| **测试结果** | 待测试 | - | ⏳ |

## 测试结果汇总

### 已完成测试
- ✅ 环境配置验证
- ✅ API代理配置验证
- ✅ 类型定义对齐验证
- ✅ 认证流程验证

### 待测试项目
- ⏳ 用户注册流程测试
- ⏳ 用户登录流程测试
- ⏳ Token刷新机制测试
- ⏳ 岗位列表获取测试
- ⏳ 岗位详情获取测试
- ⏳ 题目列表获取测试
- ⏳ 简历上传测试
- ⏳ 首页数据获取测试

## 注意事项

1. **认证机制**: 前端使用双Token机制（Access Token + Refresh Token），与后端完全匹配
2. **CORS配置**: 后端已配置允许前端开发环境地址，无需额外配置
3. **错误处理**: 前端已统一处理HTTP错误状态码，与后端错误码对应
4. **分页格式**: 前后端分页格式完全一致，无需转换
5. **数据类型**: 所有接口参数和响应数据类型已完全对齐

## 更新日志

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2024-01-20 | v1.0 | 初始版本，完成接口对照表创建 |
| 2024-01-20 | v1.1 | 完成所有接口对齐验证 |