# 前端登录注册问题诊断与修复指南

## 已修复的问题

### 1. API 路径不匹配 ✅
**问题描述**: 前端API调用缺少 `/api/v1` 前缀，导致404错误

**修复内容**: 
- 已更新 `frontend/src/services/api.ts` 中所有API路径
- 添加了 `/api/v1` 前缀到所有接口调用

**修改示例**:
```typescript
// 修复前
http.post('/auth/login', data)

// 修复后
http.post('/api/v1/auth/login', data)
```

---

## 诊断步骤

### 步骤 1: 检查后端服务是否运行

运行以下命令测试后端连接：
```bash
python test_api_connection.py
```

或者手动检查：
- 访问 http://localhost:8002/health 应该返回 `{"status": "ok", "version": "1.0.0"}`
- 如果没有响应，需要启动后端服务

### 步骤 2: 启动后端服务

如果后端未运行，执行：
```bash
cd "C:\Users\Administrator\Desktop\ai assume"
python run.py
```

预期输出：
```
INFO:     Started server process [xxxx]
INFO:     Waiting for application startup.
数据库表已创建（开发模式）
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8002 (Press CTRL+C to quit)
```

### 步骤 3: 检查数据库配置

确保 `.env` 文件中的数据库配置正确：
```env
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=mysql80
MYSQL_DB=ai_job
```

**重要**: MySQL 服务必须正在运行，且数据库 `ai_job` 必须存在。

创建数据库（如果不存在）：
```sql
CREATE DATABASE IF NOT EXISTS ai_job CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 步骤 4: 检查依赖安装

确保所有依赖已安装：

**后端依赖**:
```bash
pip install -r requirements.txt
```

**前端依赖**:
```bash
cd frontend
npm install
```

### 步骤 5: 启动前端服务

```bash
cd frontend
npm run dev
```

前端应该在 http://localhost:5173 运行

---

## 常见问题及解决方案

### 问题 1: 后端连接被拒绝
**症状**: 前端显示网络错误或超时

**解决方案**:
1. 确认后端服务正在运行
2. 检查端口 8002 是否被占用
3. 检查防火墙设置

### 问题 2: 数据库连接失败
**症状**: 后端启动时显示数据库连接错误

**解决方案**:
1. 确认 MySQL 服务正在运行
2. 检查 `.env` 中的数据库凭据
3. 确保数据库 `ai_job` 已创建
4. 检查用户权限

### 问题 3: CORS 错误
**症状**: 浏览器控制台显示 CORS 策略阻止请求

**解决方案**:
已在后端配置 CORS，允许来自 `http://localhost:5173` 的请求。
检查 `app/core/config.py` 中的 `CORS_ORIGINS` 配置。

### 问题 4: 注册时用户名或邮箱已存在
**症状**: 注册失败，提示"用户名已存在"或"邮箱已被注册"

**解决方案**:
使用不同的用户名和邮箱，或清除数据库中已有的测试数据。

### 问题 5: Token 验证失败
**症状**: 登录后立即被登出，或无法访问受保护的接口

**解决方案**:
1. 检查 `SECRET_KEY` 在前后端是否一致
2. 清除浏览器 localStorage 后重新登录
3. 检查系统时间是否正确

---

## 验证修复

### 测试注册功能
1. 访问 http://localhost:5173/auth/register
2. 填写注册表单：
   - 用户名: testuser
   - 邮箱: test@example.com
   - 密码: 123456
3. 点击注册按钮
4. 应该显示"注册成功"并跳转到登录页面

### 测试登录功能
1. 访问 http://localhost:5173/auth/login
2. 输入用户名和密码
3. 点击登录按钮
4. 应该显示"登录成功"并跳转到仪表盘

### 使用 API 测试工具
也可以使用 Postman 或 curl 直接测试 API：

```bash
# 测试注册
curl -X POST http://localhost:8002/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@example.com","password":"123456"}'

# 测试登录
curl -X POST http://localhost:8002/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456"}'
```

---

## 检查清单

- [ ] MySQL 服务正在运行
- [ ] 数据库 `ai_job` 已创建
- [ ] 后端依赖已安装 (`pip install -r requirements.txt`)
- [ ] 前端依赖已安装 (`npm install`)
- [ ] 后端服务正在运行 (端口 8002)
- [ ] 前端服务正在运行 (端口 5173)
- [ ] API 路径已更新为 `/api/v1/*`
- [ ] CORS 配置正确
- [ ] `.env` 文件配置正确
- [ ] 能够访问 http://localhost:8002/health

---

## 获取帮助

如果问题仍然存在，请提供以下信息：
1. 后端启动日志
2. 前端控制台错误信息
3. 浏览器 Network 标签中的请求详情
4. `test_api_connection.py` 的输出结果
