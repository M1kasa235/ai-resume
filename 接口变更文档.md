# 接口变更文档

## 变更概述

本次接口变更主要针对前后端数据格式不一致的问题，确保前端和后端的数据结构完全对齐。

## 变更时间
2024年1月1日

## 变更类型
- 数据结构修复
- 接口格式统一
- 前端数据访问路径修正

## 详细变更

### 1. 后端接口变更

#### 1.1 用户详情接口
**文件**：`app/schemas/user.py`

**变更前**：
```python
class UserResponse(UserBase):
    """用户详情响应"""
    model_config = ConfigDict(from_attributes=True)

    id: int
    avatar_url: Optional[str] = None
    real_name: Optional[str] = None
    gender: Optional[str] = None
    current_city: Optional[str] = None
    target_city: Optional[str] = None
    work_years: Optional[int] = None
    education: Optional[str] = None
    is_active: bool
    created_at: datetime
    last_login_at: Optional[datetime] = None
```

**变更后**：
```python
class UserResponse(UserBase):
    """用户详情响应"""
    model_config = ConfigDict(from_attributes=True)

    id: int
    avatar_url: Optional[str] = None
    real_name: Optional[str] = None
    gender: Optional[str] = None
    current_city: Optional[str] = None
    target_city: Optional[str] = None
    work_years: Optional[int] = None
    education: Optional[str] = None
    is_active: bool
    is_admin: bool  # 新增字段
    created_at: datetime
    last_login_at: Optional[datetime] = None
```

**影响**：前端现在可以正确获取用户的管理员状态

#### 1.2 工作台投递记录接口
**文件**：`app/schemas/workbench.py`

**变更前**：
```python
class ApplicationListResponse(BaseModel):
    """投递记录列表响应模型"""
    total: int
    page: int
    size: int  # 字段名不一致
    items: List[ApplicationItem]
```

**变更后**：
```python
class ApplicationListResponse(BaseModel):
    """投递记录列表响应模型"""
    total: int
    page: int
    page_size: int  # 修复字段名
    items: List[ApplicationItem]
```

**影响**：前端现在可以正确访问分页数据

### 2. 前端接口变更

#### 2.1 岗位列表数据访问
**文件**：`frontend/src/features/jobs/index.tsx`

**变更前**：
```typescript
<Badge count={jobsData?.data?.total || 0} />
{jobsData?.data?.items?.length ? (
  <Row gutter={[16, 16]}>
    {jobsData.data.items.map((job: Job) => (
```

**变更后**：
```typescript
<Badge count={jobsData?.total || 0} />
{jobsData?.items?.length ? (
  <Row gutter={[16, 16]}>
    {jobsData.items.map((job: Job) => (
```

**影响**：岗位列表现在可以正确显示总数和岗位数据

#### 2.2 题目列表数据访问
**文件**：`frontend/src/features/questions/index.tsx`

**变更前**：
```typescript
) : questionsData?.data?.items?.length ? (
  <Row gutter={[16, 16]}>
    {questionsData.data.items.map(renderQuestionCard)}
) : wrongQuestions?.data?.items?.length ? (
  <Row gutter={[16, 16]}>
    {wrongQuestions.data.items.map((item: WrongQuestionItem) => (
```

**变更后**：
```typescript
) : questionsData?.items?.length ? (
  <Row gutter={[16, 16]}>
    {questionsData.items.map(renderQuestionCard)}
) : wrongQuestions?.items?.length ? (
  <Row gutter={[16, 16]}>
    {wrongQuestions.items.map((item: WrongQuestionItem) => (
```

**影响**：题目列表现在可以正确显示题目和错题数据

#### 2.3 工作台投递记录数据访问
**文件**：`frontend/src/features/workbench/index.tsx`

**变更前**：
```typescript
applicationsData?.data?.items?.forEach((app: Application) => {
```

**变更后**：
```typescript
applicationsData?.items?.forEach((app: Application) => {
```

**影响**：工作台现在可以正确显示投递记录

## API 接口清单

### 1. 认证相关接口

#### 1.1 用户注册
- **路径**：`POST /api/v1/auth/register`
- **请求**：`UserCreate`
- **响应**：`UserResponse`
- **变更**：无

#### 1.2 用户登录
- **路径**：`POST /api/v1/auth/login`
- **请求**：`UserLogin`
- **响应**：`TokenResponse`
- **变更**：无

#### 1.3 获取当前用户
- **路径**：`GET /api/v1/auth/me`
- **请求**：无
- **响应**：`UserResponse`
- **变更**：新增 `is_admin` 字段

### 2. 岗位相关接口

#### 2.1 获取岗位列表
- **路径**：`GET /api/v1/jobs/list`
- **请求**：`JobSearchQuery`
- **响应**：`PageResponse[JobResponse]`
- **变更**：无

#### 2.2 获取热门岗位
- **路径**：`GET /api/v1/jobs/hot`
- **请求**：无
- **响应**：`List[JobResponse]`
- **变更**：无

#### 2.3 获取最新岗位
- **路径**：`GET /api/v1/jobs/latest`
- **请求**：无
- **响应**：`List[JobResponse]`
- **变更**：无

#### 2.4 获取岗位统计
- **路径**：`GET /api/v1/jobs/statistics`
- **请求**：无
- **响应**：`JobStatisticsResponse`
- **变更**：无

#### 2.5 获取岗位详情
- **路径**：`GET /api/v1/jobs/{job_id}`
- **请求**：无
- **响应**：`JobResponse`
- **变更**：无

#### 2.6 获取岗位分类树
- **路径**：`GET /api/v1/jobs/categories/tree`
- **请求**：无
- **响应**：`List[CategoryTreeItem]`
- **变更**：无

#### 2.7 获取岗位分类列表
- **路径**：`GET /api/v1/jobs/categories/list`
- **请求**：无
- **响应**：`List[CategoryItem]`
- **变更**：无

#### 2.8 收藏岗位
- **路径**：`POST /api/v1/jobs/{job_id}/favorite`
- **请求**：无
- **响应**：`MessageResponse`
- **变更**：无

#### 2.9 获取用户收藏
- **路径**：`GET /api/v1/jobs/user/favorites`
- **请求**：无
- **响应**：`PageResponse[JobResponse]`
- **变更**：无

### 3. 题目相关接口

#### 3.1 获取题目列表
- **路径**：`GET /api/v1/questions/list`
- **请求**：`QuestionSearchQuery`
- **响应**：`PageResponse[QuestionResponse]`
- **变更**：无

#### 3.2 获取题目详情
- **路径**：`GET /api/v1/questions/{question_id}`
- **请求**：无
- **响应**：`QuestionDetailResponse`
- **变更**：无

#### 3.3 获取错题记录
- **路径**：`GET /api/v1/questions/user/wrong`
- **请求**：无
- **响应**：`PageResponse[WrongQuestionItem]`
- **变更**：无

### 4. 工作台相关接口

#### 4.1 获取投递记录
- **路径**：`GET /api/v1/workbench/applications`
- **请求**：`ApplicationSearchQuery`
- **响应**：`ApplicationListResponse`
- **变更**：`size` → `page_size`

#### 4.2 获取投递详情
- **路径**：`GET /api/v1/workbench/applications/{application_id}`
- **请求**：无
- **响应**：`ApplicationDetailResponse`
- **变更**：无

### 5. 仪表板相关接口

#### 5.1 获取仪表板数据
- **路径**：`GET /api/v1/dashboard`
- **请求**：无
- **响应**：`DashboardResponse`
- **变更**：无

#### 5.2 获取成长曲线
- **路径**：`GET /api/v1/dashboard/growth-curve`
- **请求**：无
- **响应**：`GrowthCurveResponse`
- **变更**：无

#### 5.3 获取最新动态
- **路径**：`GET /api/v1/dashboard/recent-activities`
- **请求**：无
- **响应**：`List[ActivityItem]`
- **变更**：无

## 数据结构定义

### 1. 用户相关数据结构

#### UserResponse
```typescript
interface UserResponse {
  id: number;
  username: string;
  email: string;
  phone?: string;
  avatar_url?: string;
  real_name?: string;
  gender?: string;
  current_city?: string;
  target_city?: string;
  work_years?: number;
  education?: string;
  is_active: boolean;
  is_admin: boolean;  // 新增字段
  created_at: string;
  last_login_at?: string;
}
```

### 2. 岗位相关数据结构

#### JobResponse
```typescript
interface JobResponse {
  id: number;
  title: string;
  company_name: string;
  description?: string;
  requirements?: string;
  salary_min: number;
  salary_max: number;
  city: string;
  experience_min: number;
  experience_max: number;
  education_requirement: string;
  is_urgent: boolean;
  view_count: number;
  apply_count: number;
  is_active: boolean;
  published_at: string;
  created_at: string;
  updated_at: string;
  salary_display: string;
  experience_display: string;
  is_favorited: boolean;
}
```

#### PageResponse
```typescript
interface PageResponse<T> {
  total: number;
  items: T[];
  page: number;
  page_size: number;
}
```

### 3. 题目相关数据结构

#### QuestionResponse
```typescript
interface QuestionResponse {
  id: number;
  title: string;
  description: string;
  options: string[];
  correct_answer: number;
  explanation?: string;
  difficulty: 'easy' | 'medium' | 'hard';
  category: string;
  created_at: string;
  updated_at: string;
}
```

### 4. 工作台相关数据结构

#### ApplicationListResponse
```typescript
interface ApplicationListResponse {
  total: number;
  page: number;
  page_size: number;  // 修复字段名
  items: ApplicationItem[];
}
```

#### ApplicationItem
```typescript
interface ApplicationItem {
  id: number;
  job_id: number;
  job_title: string;
  company_name: string;
  status: 'pending' | 'viewed' | 'rejected' | 'accepted';
  applied_at: string;
  feedback?: string;
}
```

## 测试验证

### 1. 后端测试
- **认证接口**：注册、登录、获取用户信息
- **岗位接口**：列表、搜索、详情、收藏
- **题目接口**：列表、详情、错题记录
- **工作台接口**：投递记录、详情

### 2. 前端测试
- **组件测试**：岗位列表、题目列表、工作台
- **数据访问**：分页数据、错误处理
- **端到端测试**：用户操作流程

### 3. 契约测试
- **API响应格式**：确保前后端数据结构一致
- **错误处理**：确保错误格式统一
- **分页格式**：确保分页数据格式正确

## 部署注意事项

### 1. 数据库变更
- 无数据库schema变更
- 无数据迁移需求

### 2. 前端部署
- 需要重新构建前端代码
- 需要清除浏览器缓存

### 3. 后端部署
- 需要重启后端服务
- 需要验证API响应格式

## 回滚方案

### 1. 前端回滚
- 恢复原始代码
- 重新构建部署

### 2. 后端回滚
- 恢复原始schema
- 重启服务

## 监控指标

### 1. API响应时间
- 目标：≤ 1.5秒
- 监控：Prometheus + Grafana

### 2. 错误率
- 目标：≤ 0.1%
- 监控：Sentry

### 3. 数据一致性
- 目标：100%
- 监控：日志分析

## 总结

本次接口变更主要解决了前后端数据格式不一致的问题，确保了：

1. **数据结构对齐**：前后端数据格式完全一致
2. **接口格式统一**：标准化API响应格式
3. **前端数据访问**：修正数据访问路径
4. **测试覆盖**：建立完整的测试体系

所有变更已通过测试验证，可以安全部署到生产环境。